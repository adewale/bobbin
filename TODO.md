# TODO

## UX improvements

- [ ] **Chunk titles**: Generate topic labels instead of truncating the first sentence. Requires either LLM-generated titles or a heuristic that extracts the core concept. E.g., "Selling software vs. selling outcomes" instead of "As a software provider, are you selling the..."

- [ ] **Tag tiers**: Surface a few big themes at the top (llms, software, systems, trust) with everything else below. Or cluster tags into groups by co-occurrence.

- [ ] **Concordance visualization**: Replace the flat word table with a word cloud, treemap, or bubble chart where size = frequency and proximity = co-occurrence.

- [ ] **Reading flow**: Add "next observation" / "keep reading" navigation on chunk pages that pulls readers through the corpus instead of dead-ending.
